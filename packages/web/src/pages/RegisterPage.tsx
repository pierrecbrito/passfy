import React from 'react';
import { LoginPage } from './LoginPage';

export const RegisterPage: React.FC = () => {
  // Renders the modern split-card authentication interface with registration mode
  return <LoginPage />;
};
